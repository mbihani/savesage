# Installed by Databricks Apps at deploy time; these CANNOT be installed locally.
langgraph==0.6.4
langchain-core==0.3.74
mlflow[databricks]==3.2.0
fastapi==0.116.1
uvicorn==0.35.0
psycopg[binary]==3.2.9
# Bumped from 0.63.0: the Lakebase GenerateDatabaseCredential API now
# requires a `request_id` field that 0.63.0 doesn't send (server returns
# "Field request_id must be defined for API GenerateDatabaseCredential"),
# blocking result-store init and verdict persist. 0.82.0 sends it
# (verified live: generate_database_credential + get_endpoint both succeed).
databricks-sdk==0.82.0
python-multipart==0.0.20
